# HTTP module

Provides lightweight HTTP client/server helpers for Embedded Journey projects, including public headers in `inc/` and implementations in `src/`.

## Contents

- `inc/`: Public HTTP headers.
- `src/`: HTTP helper implementations.

